services:
  relay:
    build:
      context: ..
      dockerfile: relay/Dockerfile
    restart: unless-stopped
    ports:
      - "8080:8080"
      - "443:443"
    volumes:
      - ./relay.toml:/etc/tethera/relay.toml:ro
      - acme-cache:/var/lib/tethera/acme

volumes:
  acme-cache:
