{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities the main window holds on every platform.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
