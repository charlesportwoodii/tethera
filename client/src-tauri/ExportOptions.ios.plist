<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  The template `.github/workflows/build-ios.yml` copies to
  `gen/apple/ExportOptions.plist`, substituting the two placeholders. It lives
  here rather than in the workflow because `gen/` is regenerated by
  `tauri ios init` on every CI run and cannot hold a committed file.

  signingStyle is manual for the same reason `ios-manual-signing.mjs` exists:
  a runner has a certificate and one profile, not an Apple ID session.
-->
<plist version="1.0">
<dict>
    <key>method</key>
    <string>app-store</string>
    <key>signingStyle</key>
    <string>manual</string>
    <key>signingCertificate</key>
    <string>Apple Distribution</string>
    <key>teamID</key>
    <string>__TEAM_ID__</string>
    <key>provisioningProfiles</key>
    <dict>
        <key>com.alaydriem.tethera</key>
        <string>__PROFILE_UUID__</string>
    </dict>
</dict>
</plist>
